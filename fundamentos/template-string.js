const nome = "Rebeca"
const concatencacao = 'Olá' + nome + '!'

// Template string
const template = `Olá 
${nome} !`

console.log(template)
console.log(`1 + 1 = ${1 + 1}`)

const up = u => u.toUpperCase()
console.log(`Ei.. ${up('cuidado')}!`)
